server:
  port: 8080
spring:
  application:
    name: weather
  data:
    redis:
      host: localhost
      port: 6379

app:
  api-key: ${API_KEY:}
  api-url: ${API_URL:https://api.openweathermap.org/data/2.5/weather}